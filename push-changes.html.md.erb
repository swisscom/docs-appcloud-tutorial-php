---
title: Push Local Changes
owner: David Weber
---

<strong><%= modified_date %></strong>

In this step you’ll learn how to propagate a local change to the application through to <%= vars.product_short %>. As a simple change, we'll modify the title which the app shows.

Change the json output for url `/version` in the `app/Http/routes.php` file to show the framework version. Your final result should look something like this:

```php
25. $app->get('/version', function () use ($app) {
26.    return response()->json([ 'version' => $app->version()]);
27. });
```

Now test locally:

<pre class="terminal">
$ php -S localhost:8000 -t public
</pre>

Visiting your application at <a href="http://localhost:8000/version" target="_blank">http://localhost:8000/version</a>, you should see the framework version. **You are awesome!**

Now deploy. All you need to do is `cf push` again:

<pre class="terminal">
$ cf push my-php-app
</pre>

Finally, check that everything is working by visiting your app's URL from your web browser.

<div style="text-align:center;margin:3em;">
  <a href="./environment.html" class="btn btn-primary">I can push local changes</a>
</div>
